# FootballHub - Application React + Vite

## 🚀 Comment Lancer le Projet

### Installation

```bash
# 1. Naviguer dans le dossier
cd football-hub

# 2. Installer les dépendances
npm install

# 3. Lancer le serveur de développement
npm run dev
```

L'application sera disponible sur `http://localhost:5173` (port par défaut de Vite).

## 📦 Ce qui a été créé

- **Stack Technique**: React 18, Vite, TailwindCSS
- **Design System**: Thème Gold/Charcoal premium, Glassmorphism
- **Routing**: React Router v6 avec 6 pages principales
- **Données**: Mock data complète pour simuler le backend

## 📱 Pages Implémentées

1. **Home (`/`)**: Dashboard avec matchs en direct et prédictions
2. **AI Agent (`/ai-agent`)**: Interface de chat avec l'IA pour prédictions
3. **Stats (`/stats`)**: Analytique des matchs et probabilités
4. **Chat (`/chat`)**: Salles de discussion communautaires
5. **Profile (`/profile`)**: Gestion de profil et user tier
6. **Subscription (`/subscription`)**: Plans d'abonnement (Free, Pro, Elite Gold)

## 🎨 Personnalisation

Le thème est centralisé dans `tailwind.config.js`.
Modifiez les couleurs `gold` et `charcoal` pour ajuster l'identité visuelle.
